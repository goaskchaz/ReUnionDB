# Tables

This folder contains the SQL scripts used to create and modify the tables within ReUnionDB.

Each script should contain the complete table definition using `CREATE TABLE` or `CREATE OR ALTER` statements where appropriate.

## Naming Convention

- 
- 
- 

## Purpose

Tables store the permanent business data used throughout the database.
